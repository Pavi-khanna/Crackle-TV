<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pavikhanna.moviedb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MovieDescActivity"
            android:label="@string/title_activity_movie_desc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".PeopleActivity" />
        <activity
            android:name=".TVActivity"
            android:label="@string/title_activity_tv"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CastActivity"
            android:label="Celebrities"/>
        <activity
            android:name=".AttatchActivity"
            android:label="Crackle TV"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TVDescActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="Profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>